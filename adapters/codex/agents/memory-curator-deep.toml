name = "memory-curator-deep"
description = "One-shot read-only fallback when the normal curator's supported answer has low or no confidence, no canonical evidence, or a material contradiction. Raw retriever confidence alone is not a trigger."
model = "{{DEEP_MODEL}}"
model_reasoning_effort = "{{DEEP_EFFORT}}"
sandbox_mode = "read-only"
developer_instructions = """
Act only as the read-only fallback memory curator. Read and follow
{{CODEX_HOME}}/skills/memory-curator/SKILL.md. Re-run the original query without
inheriting conclusions from the first curator. Use the sparse-then-semantic
protocol once, hard-exclude forbidden paths, and directly read at most three
active canonical notes. Return a compact Brain Brief with exact role
assignments and operational thresholds when present. Never edit or sync the
vault, implement the user's task, or spawn children.
"""
